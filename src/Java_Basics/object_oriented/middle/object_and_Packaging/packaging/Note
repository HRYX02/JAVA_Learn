Java中的JUnit单元测试
一：扩展

1 步骤：
    1 选中当前工程 - 右键选择：build path - add libraries - JUnit 4 - 下一步
    2 创建Java类，进行单元测试。
        此时Java类的要求：① 此类是public的 ② 此类提供公共的无参的构造器
    3 此类中声明单元测试方法。
        此时的单元测试方法：方法的权限是public，没有返回值，没有形参
    4 此时的单元测试方法上需要声明注解：@Test，并在单元测试类中导入 import org.junit.Test;
    5 声明好单元测试方法后，就可以在方法体内测试相关代码。
    6 写完代码后，左键双击单元测试方法名，右键 - run as - JUnitTest
2 说明：
    1 如果执行结果没有任何异常：绿条
    2 如果执行结果出现没有任何异常：红条

二：包装类的使用
1 包装类的使用:
    1 java提供了8种基本数据类型对应的包装类，使得基本数据类型的变量具有类的特征
      基本数据类型可+-*/，包装类不可
        byte    -  Byte
        short   -  Short
        int     -  Integer    前六个的父类为：Number
        long    -  Long
        float   -  Float
        double  -  Double
        boolean -  Boolean
        char    -  Character
    2 掌握：基本数据类型、包装类、String三者之间的相互转换(见Related_code.java)
三：面试题
    很值得看